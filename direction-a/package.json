{
  "name": "direction-a",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "serve": "npx serve ."
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
